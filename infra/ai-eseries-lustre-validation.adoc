---
sidebar: sidebar
permalink: infra/ai-eseries-lustre-validation.html
keywords: Lustre, NetApp E-Series, EF80, sizing, scaling, building block, validation, performance
summary: Size Lustre with NetApp E-Series Storage building blocks, decide when to scale, and review the performance validation methodology.
---

= Lustre with NetApp E-Series Storage - Validation and Sizing Guidance
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ../media/

[.lead]
Size base and additional building blocks, decide when to add OST-only or MDT+OST capacity, and review the methodology used to validate Lustre with NetApp E-Series Storage performance.

== Base building block capacity

A base building block with two EF80 arrays and TLC NVMe drives provides the following Lustre target layout. For a diagram of preferred target placement and NVMe-oF paths, see link:ai-eseries-lustre-hw.html#_primary_building_block_volume_distribution[Primary building block volume distribution] in link:ai-eseries-lustre-hw.html[Hardware components].

The following table summarizes typical usable capacity for a base EF80 building block.

|===
| Component | Count | Typical usable capacity (per target)

| MGS
| 1
| 5–10 GiB

| MDT
| 8
| Approximately 1–2 TiB each

| OST
| 32
| Approximately 22 TiB each (TLC, RAID 6 layout)
|===

EF600 building blocks use the same MGS and MDT counts but sixteen OSTs per building block. See link:ai-eseries-lustre-architecture.html[Solution architecture] for platform-specific target counts.

Usable capacity depends on drive capacity, RAID level, and volume allocation percentages in the Ansible inventory. Use the volume sizing percentages in the deployment template as a starting point and adjust for overprovisioning requirements.

For array-level performance specifications, see the link:https://www.netapp.com/pdf.html?item=/media/14036-ds-4082.pdf[NetApp EF-Series all-flash array datasheet^]. EF80 supports up to 5,000,000 IOPS and 57 GB/s write bandwidth per array under peak conditions.

== Scaling decisions

=== Add an OST-only building block when:

* File count and metadata load are within capacity of existing MDTs
* Additional storage capacity and aggregate read/write throughput are required
* `mdt.*.md_stats` on existing MDTs show no metadata saturation

An OST-only building block adds 32 OSTs on EF80 (16 on EF600) and two OSS/MDS server nodes without adding MGS or MDT capacity.

=== Add an MDT+OST building block when:

* File count or metadata operation rate is approaching MDT capacity
* `mdt.*.md_stats` on existing MDTs show elevated operation rates or latency
* Both additional metadata throughput and data capacity are required

An MDT+OST building block adds 8 MDTs and 32 OSTs on EF80 (16 OSTs on EF600).

=== Pacemaker cluster limits

Limit each Pacemaker/Corosync cluster to five building blocks (ten OSS/MDS server nodes). For larger deployments, split building blocks evenly across multiple HA clusters. See link:ai-eseries-lustre-architecture.html[Solution architecture] for scaling rules.

== Scaling example

The following example shows a file system with two EF80 building blocks in a single HA cluster. The table lists target counts for a base building block plus an OST-only building block.

|===
| Building block | Type | Servers | Arrays | MGS | MDT | OST

| BB1
| Base
| 2
| 2
| 1
| 8
| 32

| BB2
| OST-only
| 2
| 2
| 0
| 0
| 32

| Total
|
| 4
| 4
| 1
| 8
| 64
|===

BB2 registers its OST targets against the MGS in BB1 using `mgsnode=`. OST indices for BB2 are assigned globally (for example, OST0032 through OST0063) so that no index conflicts with BB1.

== Validation methodology

Formal validation testing uses industry-standard HPC I/O benchmarks to characterize throughput, IOPS, and metadata performance:

* *IOR* - sequential and random read/write throughput at scale
* *mdtest* - metadata operation rates (create, stat, remove)
* *fio* - single-node and multi-node block-level characterization

Tests run from Lustre clients connected to the frontend fabric, with enough clients to saturate OST aggregate bandwidth. Tests include HA failover validation to confirm target recovery within acceptable service interruption windows.

== Validation results

NOTE: Validation results, including measured throughput, metadata rates, and scalability data, will be published here after formal testing is complete.

== Sizing guidance

Use the following questions when sizing a deployment:

. Determine total usable capacity required today and at full scale-out.
. Estimate file count and metadata operation rate to determine MDT count.
. Calculate the number of base and additional building blocks required.
. Confirm that the total building block count per HA cluster does not exceed five.
. Confirm that server, HCA, and array models appear in IMT for *E-Series Lustre*.
. Select TLC (RAID 6) or QLC (DDP) layout based on drive type.

For drive layout and volume counts, see link:ai-eseries-lustre-hw.html[Hardware components]. For a field-level inventory checklist, see the deployment template README in the ansible-lustre collection (a standalone checklist will be published on GitHub). For deployment steps, see link:ai-eseries-lustre-deploy.html[Deploy the solution].
